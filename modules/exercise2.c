#include <stdio.h>








// int main(){
//     for (row = n−1; row >= 0; row−−) {
// x[row] = b[row];
// for (col = row+1; col < n; col++)
// x[row] −= A[row][col]*x[col];
// x[row] /= A[row][row];
// }
// }